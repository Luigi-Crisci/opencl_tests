cmake_minimum_required(VERSION 3.7)
project(vec_add_svm LANGUAGES CXX)
find_package(OpenCL REQUIRED)
add_executable(${PROJECT_NAME} vec_add_svm.cc)
target_link_libraries(${PROJECT_NAME} PRIVATE OpenCL::OpenCL)